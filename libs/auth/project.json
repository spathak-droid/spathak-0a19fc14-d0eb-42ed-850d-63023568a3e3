{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/auth/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/out-tsc/libs/auth"],
      "options": {
        "command": "npx tsc -p libs/auth/tsconfig.lib.json --outDir dist/out-tsc/libs/auth"
      }
    }
  }
}
